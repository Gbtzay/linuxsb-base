# 氢 / 氧 命名设计

日期：2026-08-25  
范围：给人看的产品名。代码身份不动。

## 概念

基座 **氢**，套件 **氧**，合则成水。周期表一对元素，装齐才是完整物。

## 对外文案

| 位置 | 文案 |
|---|---|
| 基座 `@name` | `LINUX.SB 氢 (LSB Hydrogen · 脚本基座)` |
| 基座 `@description` | `linux.sb 脚本基座「氢」：站点解析、统一网络出口、事件总线、存储与 UI 挂载点。与「氧」相遇成水。` |
| 套件 `@name` | `LINUX.SB 氧 (LSB Oxygen · 全家桶)` |
| 套件 `@description` | 模块清单照旧；末句 `需要先安装 LINUX.SB 氢（脚本基座）。氢氧相遇成水。` |
| 面板 / 圆钮 title | `LINUX.SB · 氢` |
| 圆钮字 | `H` |
| 壳开关 | `氢壳（左栏导航 + 顶栏）` |
| 油猴菜单 | `氢壳：关闭，回到原版界面` / `氢壳：开启` |
| 左栏 aria | `氢导航` |

「套件总览」Tab 名不改。

## 不改

`window.LSB`、`LSB_PLUGINS`、`lsb_base:` 存储键、`#lsb-shell` / `lsb-*` 类名、插件 `id`、仓库与 dist 文件名、历史 spec 文件名（`bingdang-shell`）。

## 版本

基座 +1；皮肤（壳文案）+1；套件 +1。
